{
  "name": "obra-icons-mr",
  "private": true,
  "main": "index.js",
  "scripts": {
    "generate:keywords": "pnpm -r generate:keywords",
    "generate:icons": "pnpm -r generate:icons",
    "dev:website": "pnpm --filter obra-icons-website -r dev",
    "build:website": "pnpm --filter obra-icons-website -r build",
    "package": "pnpm -r package",
    "format": "pnpm -r format",
    "build": "pnpm -r build"
  },
  "engines": {
    "node": ">=20.11",
    "pnpm": ">=8.0.0",
    "npm": "forbidden, use pnpm"
  },
  "devDependencies": {
    "@types/node": "^20.10.4"
  }
}